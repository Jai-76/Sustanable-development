# Backend environment variables
# Copy this to .env and adjust as needed

# API server
HOST=0.0.0.0
PORT=8000

# ML
MODEL_CACHE_DIR=../ml/artefacts

# Sensor simulator
SENSOR_INTERVAL_SEC=5

# MQTT (optional)
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_TOPIC_PREFIX=campus/sensors
